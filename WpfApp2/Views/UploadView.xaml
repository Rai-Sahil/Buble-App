<UserControl x:Class="WpfApp2.Views.UploadView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp2.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <TextBox x:Name="KeyName" Margin="0,5,0,10"/>
            <Button x:Name="selectButton" Content="Select Video" Click="SelectButton_Click" />
            <TextBlock x:Name="filePathTextBlock" Text="No file selected" />
            <Button x:Name="uploadButton" Content="Upload Video" Click="UploadButton_Click" />
        </StackPanel>
    </Grid>
</UserControl>
